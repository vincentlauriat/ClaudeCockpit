import SwiftUI

struct QuotasView: View {
    @Environment(CockpitStore.self) private var store
    var body: some View {
        Text("QuotasView — en construction").frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
