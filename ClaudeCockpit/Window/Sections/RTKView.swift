import SwiftUI

struct RTKView: View {
    @Environment(CockpitStore.self) private var store
    var body: some View {
        Text("RTKView — en construction").frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
