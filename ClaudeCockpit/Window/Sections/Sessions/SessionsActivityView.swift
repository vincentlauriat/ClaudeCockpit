// Placeholder — filled in by the sessions UI work.
import SwiftUI
import SessionsKit

struct SessionsActivityView: View {
    var body: some View {
        Text("En construction")
            .foregroundStyle(.secondary)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
