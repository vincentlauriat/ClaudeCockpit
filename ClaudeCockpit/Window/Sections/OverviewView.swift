import SwiftUI

struct OverviewView: View {
    @Environment(CockpitStore.self) private var store
    var body: some View {
        Text("OverviewView — en construction").frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
